name: HACS Action

on:
  push:
    branches:
      - main
  release:
    types: [published]

jobs:
  hacs:
    name: HACS Action
    runs-on: ubuntu-latest
    steps:
      - name: HACS Action
        uses: hacs/action@main
        with:
          category: integration